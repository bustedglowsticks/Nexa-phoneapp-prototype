{
  "framework": "nextjs",
  "buildCommand": "prisma generate && if [ -n \"$DATABASE_URL\" ]; then prisma migrate deploy; else echo 'Skipping prisma migrate (no DATABASE_URL)'; fi && next build",
  "installCommand": "npm install"
}
